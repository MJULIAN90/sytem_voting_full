import { colors } from "../../Assets";

const styles = {
    container: {
        background: colors.colorFont,
        height: "110px",
        boxShadow: '10px 6px 5px black'
    }
};

export default styles;
